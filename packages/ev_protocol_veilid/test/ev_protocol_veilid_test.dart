import 'package:test/test.dart';
import 'package:ev_protocol_veilid/ev_protocol_veilid.dart';

void main() {
  group('EvProtocolVeilid', () {
    test('placeholder', () {
      // Placeholder test — real tests will cover Drift + Veilid integration
      expect(EvProtocolVeilid, isNotNull);
    });
  });
}
