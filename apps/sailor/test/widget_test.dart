import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Sailor app smoke test', (WidgetTester tester) async {
    // Placeholder — real widget tests will be added per feature.
    expect(true, isTrue);
  });
}
